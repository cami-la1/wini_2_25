//---------------------------------------------------------------------------

#pragma hdrstop

#include "UP_vectores.h"
//---------------------------------------------------------------------------
#pragma package(smart_init)
	pilav:: pilav(){
	   tope=0;
	}
	bool pilav::Vacia(){
     return (tope==0);
	}
	void pilav::Meter(int e){
	 if (tope<MAX) {
	   tope=tope+1;
       elementos[tope]=e;
	 }
	}
	void pilav::Sacar(int &e){
	 if (Vacia==false) {

	 }
	}
